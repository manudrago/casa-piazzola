# Generated — do not edit by hand

Everything under `apartment/`, `lovere/` and `og/` is produced by:

    npm run images

from the sources in `/photos` and the mapping in `/scripts/curation.json`.
It is left out of the archive to keep the download small; run the command once
after `npm install` and the folders fill themselves.
